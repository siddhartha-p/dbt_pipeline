dbt_employee_pipeline:
  outputs:
    dev:
      dbname: airflow
      host: postgres
      pass: airflow
      port: 5432
      schema: silver
      threads: 4
      type: postgres
      user: airflow
  target: dev
